TopicText {   
    width: 80;
    text-align: left;
}

.selected {
    background: $success-muted;
    color: $text;
}

#intro {
    dock: top;
    padding: 1;
    width: 100%;
    height: 3;
    background: $boost;
}
