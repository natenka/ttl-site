TopicText {   
    width: 80;
    text-align: left;
    margin: 1 1;
    content-align: left middle;
}

.selected {
    background: $success-muted;
    color: $text;
}

#intro {
    dock: top;
    padding: 1;
    width: 100%;
    background: $boost;
}

#questions {
    padding: 2;
    width: 100%;
    background: $boost;
}
